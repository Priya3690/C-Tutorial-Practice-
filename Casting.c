#include<stdio.h>
int multiply (int x, int y)
{
  return x * y;
}
int main(){
	int   trips           = 6;
	float distance        = 4.874;
	//int   approxC
	Distance  = (int)distance;
	int total = multiply ( trips, distance );
	printf("Total distance is %i \n \n",total);
	return 0;
}
